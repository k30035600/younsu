**문서 색인(DOCX 빌드·제출 유의):** [`../../docs/최종본-docx-빌드.md`](../../docs/최종본-docx-빌드.md) · **저장소 경로·갑 링크 전수:** [`../../docs/경로-전수조사.md`](../../docs/경로-전수조사.md)

**갑호증 표기(포털 더블클릭):** 「증거자료 목록」 문구·편철 설명은 **청구서 정본**(`260404_01_…`)에 기술합니다. 포털·정본 MD에는 `[갑 제4-1호증](#4-1)` 형식의 링크를 둡니다. 일괄 변환: `python docs/scripts/wrap_gab_cite_links_in_md.py --apply 행정심판청구(최종)/260404_01_행정심판청구서_최종.md` 등(`docs/scripts/`).

**청구서·집행정지신청서 정본(Markdown):** 상위 폴더 `../260404_01_행정심판청구서_최종.md`, `../260404_02_집행정지신청서_최종.md`(동일 접두). **Word(DOCX)** 는 **`docs/{yymmdd}/`** 에만 둡니다(예: `docs/260404/260404_01_…_최종.docx`). 생성·갱신: `docs/scripts/260404_build_final_docx.py`, 제출용 TXT: `docs/scripts/export_submission_txt.py` → 산출은 모두 `docs/260404/` 등 **날짜 폴더**.

**역할:** 이 파일은 **작업·색인(제출 정본에 넣지 않음)**용 허브이다. **제출용 MD·별지**는 **`행정심판청구(최종)/` 직하**에 두고, 본 `260404/` 폴더에는 허브·검수·참고·갑호증 작업물을 둔다. (역할 구분은 `../260404_01_행정심판청구서_최종.md` **미주** 참조.)

제출·검수용 **정리본**은 상위 폴더의 제출 MD·아래 링크를 기준으로 하면 됩니다.

**행정심판 증거·편철·스크립트**는 **`../../행정심판청구(증거)/최종/README_행정심판_통합안내.md` 한 파일**이 정본입니다. 폴더별 짧은 색인은 **`../../행정심판청구(증거)/최종/260328_readme_증거_갑호증_편철안내.txt`** 등(같은 루트에 모음)을 보면 됩니다.

**파일명 규칙:** 제출 서면·Cursor·도구가 만든 문서는 `yymmdd_` 접두(예: `260404_`)로 시계열 관리합니다. 동일 내용을 고칠 때마다 날짜 접두를 바꾸거나 새 파일을 추가하는 방식으로 맞추면 됩니다.

**폴더 규칙(권장):** **제출·준제출 정본 MD**(청구서·집행정지·별첨 색인·집대성·시간축 별지 등)는 **`행정심판청구(최종)/` 직하**(포털 `서면/260404_01_…` 등)에 두고, **개요 허브·갑호증 검수·참고·전수조사** 등은 **`행정심판청구(최종)/260404/`** 작업 폴더(포털 개요: `서면/260404/…`)에 둡니다. **DOCX·제출 TXT 생성 스크립트는 `docs/scripts/`**, 증거·편철 등 **그 밖의 자동화는 `tools/`**에 둡니다. **재작성 이전 스냅샷**(접두 `260402`)은 형제 폴더 `행정심판청구(최종)/260403/260402/`에 두고, 루트의 `260403_01_…`·`260403_02_…` 등은 **별도 초안·작업본**으로 구분합니다. **통합 현장·참고 원본(`돌심방자료/`)**은 `선거법위반/`, `고소고발/`과 같은 층(프로젝트 루트)에 두어 교차 참조하기 쉽게 합니다.

---

## 별첨 — 배치·법령정보·검수(개요 보충)

아래는 **청구서·별첨 정본에 넣지 않는** 허브(개요) 전용 보충입니다. 포털 **개요** 탭과 동일 본문입니다.

<div style="border:1px solid #7a6a5a; border-left:4px solid #5c4d3f; padding:14px 18px; margin:12px 0 20px; background:#faf8f4; color:#222;">

<p><strong>배치.</strong> 본 파일은 프로젝트 루트의 <code>younsu/행정심판최종본/</code>에 있습니다. 같은 폴더에 <code>01_</code>~<code>03_</code> 접두의 제출용 문서가 있으며, 국가법령정보센터 요약 PDF(재추출본)는 <code>행정심판청구(증거)/최종/법령정보/</code>에 <code>사건번호_사건명.pdf</code>로 둡니다(갑호증 제출 대상 아님; <code>python tools/260402_build_prec_pdfs.py</code>). 법제처 행정기본법 질의응답 Q10·Q18 발췌는 <strong>갑 제13-1호증, 갑 제13-2호증</strong>(통칭 갑 제13호증) PDF로 갑호증(증거)에 둡니다 <em>(구 갑제16·갑제17 파일명일 수 있음)</em>. 인용 순 목차는 <code>(국가법령정보)판례모음/README_국가법령정보_인용순서.txt</code>를 참고합니다. 참고 판례(97누8540 등)·중복 정리분은 <code>행정심판청구(증거)/최종/법령정보/</code>에 둡니다(루트 <code>기타참고/</code> 폴더는 폐지). 위 세 서면(01·02·03)을 기준으로 전수 정리했습니다.</p>

<p><strong>0. 정확성·출처</strong></p>
<p><strong>법령정보(국가법령정보센터)와의 관계:</strong> <code>tools/260402_build_prec_pdfs.py</code>의 <code>CASES</code>는 국가법령정보센터(law.go.kr) 공개 판례 정보를 바탕으로 한 재추출·편집본입니다. PDF는 그 <code>CASES</code>를 한 페이지로 옮긴 것입니다.</p>
<p><strong>PDF에 실리는 범위:</strong> 판결문 전체가 아니라 판시사항·판결요지·주문(과 표제·사건명 등) 위주입니다. 포털·대법원 판례 원문과 글자 단위로 완전 일치한다고 단정할 수는 없습니다.</p>
<p><strong>제출자 측 보완·대조 점검</strong>은 <code>제출자_검수/260404_인용판례색인_제출자검수.md</code>에 둔다(제출 패키지에 넣지 않음).</p>

</div>

## 이 폴더(`260404/`)에 있는 것

| 구분 | 파일명(예시·현재 본) |
|------|----------------------|
| **개요·허브 MD(제출 제외)** | `260404_00_개요_README_younsu_허브.md`(본 파일) |
| **보조·작업 MD(제출 제외)** | `260404_참고_청구서_판례주석.md`, `260404_별지_갑호증_목록_드롭다운.md`, `260404_갑호증_검수보고.md` — 갑 폴더 **파일 전체 덤프 TXT**는 `260404_갑호증_검수보고.md` §9 스크립트로 로컬 재생성(저장 위치는 작업자 선택). |

### `행정심판청구(최종)/` 하위 `260404_*` 전수(정리 기준 2026-04-04)

| 경로 | 역할 |
|------|------|
| `260404_01_행정심판청구서_최종.md` | 제출 정본(청구서) |
| `260404_02_집행정지신청서_최종.md` | 제출 정본(집행정지) |
| `260404_00_별첨_인용판례_및_법령_색인.md` | 별첨 색인 |
| `260404_03_핵심판례_집대성.md` | 집대성(보조·제출 선택) |
| `260404_별지_사실관계_시간축_정리표.md` | 시간축 별지(MD 정본; 포털 시간축 탭) |
| `260404/260404_00_개요_README_younsu_허브.md` | 개요(포털) |
| `260404/260404_갑호증_검수보고.md` | 갑 실물 검수 |
| `260404/260404_별지_갑호증_목록_드롭다운.md` | 갑 목록 `<details>`용 |
| `260404/260404_참고_청구서_판례주석.md` | 판례 주석 분리본 |

### Word·제출 TXT(`docs/{yymmdd}/` — **행정심판청구(최종) 직하에 DOCX를 두지 않음**)

| 경로 | 역할 |
|------|------|
| `docs/260404/260404_01_행정심판청구서_최종.docx` | 청구서 Word(`260404_build_final_docx.py --01`) |
| `docs/260404/260404_02_집행정지신청서_최종.docx` | 집행정지 Word(`--02`) |
| `docs/260404/260404_*_제출용.txt` | `export_submission_txt.py` 산출(있을 때) |

**앞으로:** 새 제출 묶음은 **`docs/YYMMDD/`** 폴더를 만들고, 스크립트의 출력 경로(또는 `DOCS_OUT`)를 그 날짜로 맞춥니다.

## 상위 폴더(`행정심판청구(최종)/`)의 제출 정본

| 구분 | 파일명(예시·현재 본) |
|------|----------------------|
| **행정심판 제출 서면** | `260404_01_행정심판청구서_최종.md`, `260404_02_집행정지신청서_최종.md`, `260404_00_별첨_인용판례_및_법령_색인.md` |
| **보조(제출에 포함할 수 있음)** | `260404_03_핵심판례_집대성.md`, `260404_별지_사실관계_시간축_정리표.md` — **미주·붙임 구성**은 `260404_01_` **미주** |

### 검수·작업 기록(제출 패키지 제외)

| 구분 | 위치 |
|------|------|
| **제출자 검수** | `제출자_검수/` — 작업·검수 메모, OneDrive 검수, 집대성·색인 대조 절차. **전자제출·USB 정본에는 넣지 않음**(`제출자_검수/README.md` 참조) |
| **OneDrive 검수** | **`제출자_검수/260404_OneDrive_검수_돌심방_연수구청.md`**. 경로 덤프 TXT는 **재생성 가능** — 필요 시 `tools/260328_audit_onedrive_yeonsu.ps1` 실행 시 `행정심판최종본/OneDrive_검수_목록/`에 다시 생김 |
| **고소·고발 작업 기록** | **읽을 것:** `고소고발_작업기록/`의 **`yymmdd_본문검토_*.md` 요약**(또는 최신 MD). CSV는 건별 확인 시만. `260328_고소고발_OneDrive이동로그.txt`는 **과거 이동 이력**용으로, 상시 검토 불필요 |

## 다른 폴더(작업·원본·증거)

| 경로 | 용도 |
|------|------|
| `행정심판청구(증거)/최종/판례모음/` | 판례 PDF 등 **작업·제출용 사본**. 원칙·도구는 **`행정심판청구(증거)/최종/README_행정심판_통합안내.md`** |
| `행정심판청구(증거)/최종/갑호증(증거)/` | 갑호증 **사본**. 상세·dedupe·복사 명령은 **`행정심판청구(증거)/최종/README_행정심판_통합안내.md`** |
| `돌심방자료/` (프로젝트 루트) | 현장·통합 **원본 단일본**(행정심판·선거법·고소고발 공용). 증거로 쓸 때 **여기서 파일을 옮기지 말고** `행정심판청구(증거)/최종/갑호증(증거)/`에 **복사** (`tools/260328_copy_evidence_gab.py` 또는 수동) |
| `tools/` | **지속 사용 스크립트** (`yymmdd_*.py`, `.ps1`): `260328_organize_folders.py`, `260328_build_prec_pdfs.py`, `260328_copy_evidence_gab.py`, `260328_scan_yeonsu_dispute_content.py`, `260328_audit_onedrive_yeonsu.ps1`, `260319_build_*.py` — 돌심방 대량 통합·중복정리 등 **일회성 도구는 정리 시 제거**됨 |
| `고소고발/` | 분류된 고소·정보공개·행정심판 관련 **원본 파일** |
| `선거법위반/` | 선거법 관련 자료 |
| ~~`자료검토/`~~ | **삭제·정리됨(2026-03-28).** 판례·법리 정본은 `행정심판최종본/`의 `260404_00_별첨_인용판례_및_법령_색인.md`, `260404_03_핵심판례_집대성.md`를 사용합니다. |

## 예전 위치(정리됨)

- 구 `행정심판청구(증거)/최종/최종본/260328_서면_위치_안내.md`(옛 경로)·`행정심판청구(증거)/최종/돌심방자료_이전안내.txt` 내용은 **`../../행정심판청구(증거)/최종/README_행정심판_통합안내.md`** 및 본 허브 **「다른 폴더」** 표에 흡수하였고 파일은 제거함. 제출 서면 정본은 **`younsu/행정심판청구(최종)/`** 직하의 `260404_01_` … MD이다. 개요 허브·작업물은 **`…/260404/`**에 둔다. (과거 `260328_*`·`260330_*` 스냅샷 폴더는 정리 시 제거됨 — 필요 시 Git 기록에서 복원.)

## 재생성 가능(삭제해 둔 것)

- **2026-03-30:** `행정심판최종본/`에 있던 **MD와 동일한 내용의** `260328_OneDrive_최종검수_돌심방_연수구청.txt`, `260328_00_인용판례_및_법령_색인.txt`는 중복이라 제거(정본은 각 `.md`). `260328_01_행정심판청구서_제출용-kcs30035600.txt`는 `docs/scripts/export_submission_txt.py` 산출물과 별개 변형본이라 제거.
- 루트의 `pdf.html`, `hwp.html`, `docs.html` — `tools/260319_build_*_html.py`로 재생성. `extracted_text.txt`는 필요 시 DOCX에서 수동 추출(일회 추출용 스크립트는 제거됨).
- `tools/*.log.txt` 등 — 실행 시 다시 생성되는 로그.

---

## Cursor·스크립트가 만든 TXT 전수(요약)와 관리안

**원칙:** TXT를 늘리지 않고, **“무엇을 열어볼지”를 이 표 하나로 제한**합니다. 나머지는 재생성·보관만 구분합니다.

### 1) `OneDrive_검수_목록/` — **현재 비움(재생성 전용)**

| 내용 | 필요성 |
|------|--------|
| `OneDrive_*.txt` | `tools/260328_audit_onedrive_yeonsu.ps1` 출력물. **워크스페이스에는 두지 않음**(재생성 가능). |
| **관리** | 결론·숫자는 **`제출자_검수/260404_OneDrive_검수_돌심방_연수구청.md`**만 유지. 목록이 필요할 때만 스크립트 실행 → `행정심판최종본/OneDrive_검수_목록/` 자동 생성. |

### 2) `고소고발_작업기록/` — **일부만 열람**

| 파일 | 필요성 |
|------|--------|
| `yymmdd_본문검토_타임스탬프.md` (또는 기존 `260328_고소고발_본문검토.md`) | 스캔 **요약·재검토 권장 목록** — **이것만** 정기적으로 보면 됩니다. |
| 같은 이름대의 `.csv` | 행별로 원본과 대조할 때만. |
| `260328_고소고발_OneDrive이동로그.txt` | 파일을 어디서 옮겼는지 **감사 로그**. 분쟁·추적이 없으면 **열어볼 일 거의 없음**. |

**관리:** `python tools/260328_scan_yeonsu_dispute_content.py`를 다시 돌리면 **`yymmdd_본문검토_…`가 새로 생깁니다.** 이전 배치의 CSV/MD는 **하나만 남기고 나머지 삭제**하거나, 하위에 `_archive` 폴더를 만들어 넣어도 됩니다(스캔 스크립트는 산출물 파일명에 타임스탬프를 붙이므로 누적되기 쉬움).

### 3) 그 밖의 `younsu` 안 `.txt` (역할만 구분)

| 구역 | 대표 파일 | 분류 |
|------|-----------|------|
| ~~`자료검토/`~~ | *(구 Cursor 메모·전수조사 TXT 등은 2026-03-28 정리 시 제거. 빈 폴더는 탐색기에서 삭제 가능.)* | 법리·판례는 **`260404_00_별첨_인용판례_및_법령_색인.md`**, **`260404_03_핵심판례_집대성.md`** |
| `돌심방자료/` | `분류기준.txt`, `통합_이동_요약.txt`, `분류_복사_요약.txt`, `파일명과_사진소개.txt` | **정책·과거 작업 로그** — 분류기준은 유지; 이동/복사 요약은 이력용으로 **새로 안 늘리는 것**이 좋음. |
| `행정심판청구(증거)/최종/` | `README_행정심판_통합안내.md`, `260328_readme_증거_갑호증_편철안내.txt`, `260328_readme_판례모음_원본사본.txt` | **통합 안내 + 색인 TXT는 루트에만** — 제출 전 확인 |
| `돌심방자료/01_…` 아래 | 선거법 관련 `.txt`, `동영상 text.txt` 등 | **실질 자료** — 삭제 대상 아님. |

### 4) 추천 운영(경제적으로)

1. **매일/매주 열어볼 파일:** **제출 서면 MD** + 필요 시 **`제출자_검수/260404_OneDrive_검수_돌심방_연수구청.md`** + **최신 `본문검토` MD** 정도로 고정.  
2. **경로 덤프 TXT**는 기본 **보관하지 않음** — 증빙이 필요할 때만 `260328_audit_onedrive_yeonsu.ps1`로 **재생성**.  
3. 스캔·검수를 돌릴 때마다 생기는 산출물은 **`yymmdd_`로 시계열 관리**하고, **오래된 배치는 과감히 삭제**해 TXT 개수를 억제.  
4. 새 안내용 TXT를 **남발하지 말고**, 가능하면 **이 README(또는 기존 `분류기준.txt` 등 한두 곳)**에만 합칩니다.
